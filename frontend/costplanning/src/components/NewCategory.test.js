import React from "react"
import "@testing-library/jest-dom/extend-expect"
import { render, fireEvent } from "@testing-library/react"
import NewCategory from "./NewCategory"

test("onSubmit za novu kategoriju", () => {
    /*
    const funkcijaZaSpremanje = jest.fn()

    const komp = render(<NewCategory dodavanjeKategorije={funkcijaZaSpremanje}></NewCategory>)

    const input = komp.container.querySelector("input")
    const forma = komp.container.querySelector("form")


    fireEvent.change(input, {
        target: { value: "nova kategorija" }
    })

    fireEvent.submit(forma)

    expect(funkcijaZaSpremanje.mock.calls).toHaveLength(1)
    */
})